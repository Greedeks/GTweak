<UserControl x:Class="GTweak.Assets.UserControls.ExpandableBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Loaded="OnLoaded" Unloaded="OnUnloaded"
             d:DesignHeight="40" d:DesignWidth="140" x:Name="Root" UseLayoutRounding="True" SnapsToDevicePixels="True" 
             KeyboardNavigation.TabNavigation="None" IsTabStop="False" Focusable="False" 
             PreviewKeyDown="Popup_PreviewKeyDown">
    <Grid>
        <ToggleButton x:Name="TglArrow" Style="{StaticResource TglDropDown}" Height="31" Padding="10,5" Checked="TglArrow_Checked" Unchecked="TglArrow_Unchecked" PreviewMouseRightButtonDown="TglArrow_PreviewMouseRightButtonDown">
            <TextBlock Text="{Binding ElementName=Root, Path=Text}" VerticalAlignment="Center" FontSize="14px" FontFamily="{StaticResource Jura}" Foreground="{StaticResource Brush_Highlighted}" Margin="0,0,10,0"/>
        </ToggleButton>

        <Popup x:Name="DropdownPopup" Placement="Bottom" StaysOpen="False" AllowsTransparency="True" Opened="Popup_Opened" Closed="Popup_Closed" VerticalOffset="2" PopupAnimation="Fade">
            <Border x:Name="PopupBorder" Background="{StaticResource Brush_Background_ComboBox}" BorderBrush="{StaticResource Brush_Separator}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="10" RenderOptions.EdgeMode="Aliased">
                <Border.CacheMode>
                    <BitmapCache RenderAtScale="2" EnableClearType="True"/>
                </Border.CacheMode>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Opacity="0.15" ShadowDepth="2"/>
                </Border.Effect>
                <Border.RenderTransform>
                    <TranslateTransform x:Name="PopupTransform"/>
                </Border.RenderTransform>
                <ScrollViewer MaxHeight="{Binding ElementName=Root, Path=MaxDropdownHeight}" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ElementName=Root, Path=Items}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</UserControl>