<ui:FluentWindow x:Class="GTweak.Windows.ImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:GTweak.Windows"
        mc:Ignorable="d" Icon="/GTweak.ico"
        Background="Transparent" WindowStyle="None" IsTabStop="False" SnapsToDevicePixels="True" UseLayoutRounding="True" ShowInTaskbar="False" KeyboardNavigation.TabNavigation="None"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" SizeToContent="Width" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Title="GTweak" Topmost="True" MinHeight="135" MinWidth="310" MaxHeight="135" MaxWidth="380" ContentRendered="Window_ContentRendered">
    <Grid Background="{StaticResource Brush_Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBar" Grid.Row="0" MouseDown="TitleBar_MouseDown" Background="Transparent">
            <DockPanel VerticalAlignment="Center" HorizontalAlignment="Left" LastChildFill="True" Height="48">
                <ui:SymbolIcon Symbol="DocumentArrowDown16" FontSize="20px" Foreground="{StaticResource Brush_Accent}"  Margin="10,5,0,0" HorizontalAlignment="Center"/>
                <Viewbox Stretch="None">
                    <TextBlock Text="{DynamicResource title_import}" Style="{StaticResource TextDefault}" FontSize="18px" Margin="5,0,50,0"/>
                </Viewbox>
            </DockPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{DynamicResource warn_import}" Foreground="{StaticResource Brush_Inactive}" Height="auto" Width="auto" FontFamily="{StaticResource Jura}" FontSize="13px" HorizontalAlignment="Left" TextAlignment="Center" Margin="10,0,10,0"/>
                <StackPanel Orientation="Horizontal">
                    <ui:ProgressRing Margin="10,10,0,0" Width="25" Height="25" Foreground="{StaticResource Brush_Accent}" IsIndeterminate="True"/>
                    <TextBlock Foreground="{StaticResource Brush_Highlighted}" Height="auto" Width="auto" FontFamily="{StaticResource Jura}" FontSize="13px" Margin="10,10,0,0" HorizontalAlignment="Left" TextAlignment="Center" VerticalAlignment="Center">
                        <Run Text="{DynamicResource wait_import}" /><Run x:Name="DotRun" Text="" />
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard Timeline.DesiredFrameRate="60" RepeatBehavior="Forever" Duration="0:0:0.8">
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="DotRun" Storyboard.TargetProperty="Text">
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0"   Value="" />
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="." />
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0.4" Value=".." />
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0.6" Value="..." />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                </StackPanel>
                <TextBlock Foreground="{StaticResource Brush_Inactive}" Height="auto" Width="auto" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" FontFamily="{StaticResource Jura}" FontSize="12px" HorizontalAlignment="Left" TextAlignment="Center" Margin="10,10,0,0">
                           <Run Text="{DynamicResource file_import}"/>
                           <Run Text="“"/><Run x:Name="ImportedFile" Text="The imported file" Foreground="{StaticResource Brush_Accent}"/><Run Text="”"/>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
