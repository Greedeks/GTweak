<ui:FluentWindow x:Class="GTweak.Windows.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:GTweak.Core.ViewModel"
        xmlns:b="clr-namespace:GTweak.Behaviors"
        xmlns:uc="clr-namespace:GTweak.Assets.UserControls"
        xmlns:local="clr-namespace:GTweak.Windows"
        mc:Ignorable="d" Icon="/GTweak.ico" Title="{DynamicResource title_export}" Background="Transparent" WindowStyle="None"
        IsTabStop="False" SnapsToDevicePixels="True" UseLayoutRounding="True" KeyboardNavigation.TabNavigation="None" ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize" MinWidth="250">
    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </ui:FluentWindow.Resources>
    <ui:FluentWindow.DataContext>
        <vm:ExportViewModel />
    </ui:FluentWindow.DataContext>

    <Grid Background="{StaticResource Brush_Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBar" Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" HorizontalAlignment="Left" LastChildFill="True" VerticalAlignment="Center" Margin="14,0,0,0">
                <ui:SymbolIcon Symbol="ArrowExportUp24" FontSize="16" Foreground="{StaticResource Brush_Accent}" VerticalAlignment="Center"/>
                <TextBlock Text="{DynamicResource title_export}" Style="{StaticResource TextDefault}" FontSize="14" Margin="8,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
            </DockPanel>
            <StackPanel x:Name="TitleButtonsPanel" Grid.Column="2" Margin="0,0,0,2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                <Button x:Name="ButtonMinimize" Style="{StaticResource ButtonMinimize}" Click="ButtonMinimize_Click"/>
                <Button x:Name="ButtonMaximize" Style="{StaticResource ButtonMaximize}" Click="ButtonMaximize_Click"/>
                <Button x:Name="ButtonClose" Style="{StaticResource ButtonClose}" Margin="0,0,5,0" Click="ButtonClose_Click"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <ScrollViewer b:ScrollFadeBehavior.EnableFade="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <uc:FlowPanel Margin="10" HorizontalSpacing="10" VerticalSpacing="10" ContentAlignment="Stretch" HorizontalAlignment="Stretch" UseMasonryLayout="False" VerticalAlignment="Top">

                    <HeaderedContentControl Style="{StaticResource ExportCard}" Header="{DynamicResource rbtn_conf_main}" Tag="{Binding Confidentiality.Count}" Visibility="{Binding Confidentiality.IsVisible, Converter={StaticResource BoolToVisConverter}}">
                        <ItemsControl ItemsSource="{Binding Confidentiality.Items}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <uc:ExportItem Label="{Binding Label}" Value="{Binding Value}" RemoveCommand="{Binding RemoveCommand}" BorderBrush="{StaticResource Brush_Border_Description}" BorderThickness="0,0,0,1"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </HeaderedContentControl>

                    <HeaderedContentControl Style="{StaticResource ExportCard}" Header="{DynamicResource rbtn_int_main}" Tag="{Binding Interface.Count}" Visibility="{Binding Interface.IsVisible, Converter={StaticResource BoolToVisConverter}}">
                        <ItemsControl ItemsSource="{Binding Interface.Items}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <uc:ExportItem Label="{Binding Label}" Value="{Binding Value}" RemoveCommand="{Binding RemoveCommand}" BorderBrush="{StaticResource Brush_Border_Description}" BorderThickness="0,0,0,1"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </HeaderedContentControl>

                    <HeaderedContentControl Style="{StaticResource ExportCard}" Header="{DynamicResource rbtn_app_main}" Tag="{Binding Packages.Count}" Visibility="{Binding Packages.IsVisible, Converter={StaticResource BoolToVisConverter}}">
                        <ItemsControl ItemsSource="{Binding Packages.Items}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <uc:ExportItem Label="{Binding Label}" Value="{Binding Value}" RemoveCommand="{Binding RemoveCommand}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </HeaderedContentControl>

                    <HeaderedContentControl Style="{StaticResource ExportCard}" Header="{DynamicResource rbtn_svc_main}" Tag="{Binding Services.Count}" Visibility="{Binding Services.IsVisible, Converter={StaticResource BoolToVisConverter}}">
                        <ItemsControl ItemsSource="{Binding Services.Items}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <uc:ExportItem Label="{Binding Label}" Value="{Binding Value}" RemoveCommand="{Binding RemoveCommand}" BorderBrush="{StaticResource Brush_Border_Description}" BorderThickness="0,0,0,1"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </HeaderedContentControl>

                    <HeaderedContentControl Style="{StaticResource ExportCard}" Header="{DynamicResource rbtn_sys_main}" Tag="{Binding System.Count}" Visibility="{Binding System.IsVisible, Converter={StaticResource BoolToVisConverter}}">
                        <ItemsControl ItemsSource="{Binding System.Items}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <uc:ExportItem Label="{Binding Label}" Value="{Binding Value}" RemoveCommand="{Binding RemoveCommand}" BorderBrush="{StaticResource Brush_Border_Description}" BorderThickness="0,0,0,1"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </HeaderedContentControl>

                </uc:FlowPanel>
            </ScrollViewer>

            <Grid x:Name="EmptyStateGrid" IsHitTestVisible="False" Margin="20">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding TotalCount}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ui:SymbolIcon Opacity="0" Symbol="DocumentDismiss24" Foreground="{StaticResource Brush_TextBlock_Inactivity}" FontSize="55" Margin="5,0,5,0" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0">
                        <ui:SymbolIcon.RenderTransform>
                            <ScaleTransform ScaleX="0.5" ScaleY="0.5"/>
                        </ui:SymbolIcon.RenderTransform>
                        <ui:SymbolIcon.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard Timeline.DesiredFrameRate="120">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6" BeginTime="0:0:0.1">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" From="0.5" To="1" Duration="0:0:0.5" DecelerationRatio="0.5" BeginTime="0:0:0.1">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" From="0.5" To="1" Duration="0:0:0.5" DecelerationRatio="0.5" BeginTime="0:0:0.1">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ui:SymbolIcon.Triggers>
                    </ui:SymbolIcon>

                    <TextBlock x:Name="EmptyStateText" Width="Auto" Height="Auto" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{StaticResource Jura}" FontSize="17pt" Foreground="{StaticResource Brush_TextBlock_Inactivity}" Grid.Row="1" Margin="5,15,5,0" Opacity="0" RenderTransformOrigin="0.5,0.5">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="20"/>
                        </TextBlock.RenderTransform>
                        <Run Text="{DynamicResource title_empty_export}"/><LineBreak/>
                        <Run FontSize="14pt" Foreground="{StaticResource Brush_Inactive}" Text="{DynamicResource desc_empty_export}"/>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard Timeline.DesiredFrameRate="120">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6" BeginTime="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y" From="20" To="0" Duration="0:0:0.5" DecelerationRatio="0.7" BeginTime="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                </Grid>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource Brush_Background_NavPanel}" BorderBrush="{StaticResource Brush_Border_Description}" BorderThickness="0,1,0,0" Padding="16,10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding TotalCount}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Style="{StaticResource TextDefault}"  Foreground="{StaticResource Brush_TextBlock_Inactivity}" FontSize="13" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,10,0">
                    <Run Text="{DynamicResource label_total_export}"/>
                    <Run Text="{Binding TotalCount, Mode=OneWay}" Foreground="{StaticResource Brush_Accent}" FontFamily="{StaticResource Jura}"/>
                </TextBlock>

                <Button Grid.Column="1" Style="{StaticResource ButtonContent}" HorizontalAlignment="Right" VerticalAlignment="Center" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="DocumentSave20" FontSize="14" Margin="0,0,6,0"/>
                        <TextBlock VerticalAlignment="Center" Text="{DynamicResource btn_save}" FontSize="13"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>