<ui:FluentWindow x:Class="GTweak.Windows.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:GTweak.Windows"
        mc:Ignorable="d" Icon="/GTweak.ico"
        Background="Transparent" WindowStyle="None" IsTabStop="False" SnapsToDevicePixels="True" UseLayoutRounding="True" KeyboardNavigation.TabNavigation="None"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" SizeToContent="Height" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Title="{DynamicResource title_win_update}" Topmost="True" MaxWidth="400" MaxHeight="250" MinHeight="120" MinWidth="360" Loaded="Window_Loaded">
    <Grid Background="{StaticResource Brush_Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" MouseDown="TitleBar_MouseDown" Background="Transparent">
            <DockPanel VerticalAlignment="Center" HorizontalAlignment="Left" LastChildFill="True" Height="48">
                <ui:SymbolIcon Symbol="CloudArrowDown24" FontSize="20px" Foreground="{StaticResource Brush_Accent}"  Margin="15,5,0,0" HorizontalAlignment="Center"/>
                <Viewbox Stretch="None">
                    <TextBlock Style="{StaticResource TextDefault}" FontSize="18px" Margin="5,0,100,0">
                            <Run Text="{DynamicResource download_update}"/>
                            <Run Text="“"/><Run Text="GTweak" Foreground="{StaticResource Brush_Accent}"/>
                            <Run Text="{Binding DownloadVersion}" Foreground="{StaticResource Brush_Accent}"/><Run Text="”"/>
                    </TextBlock>
                </Viewbox>
            </DockPanel>
        </Grid>

        <Grid Grid.Row="1">
            <StackPanel>
                <TextBlock Text="{DynamicResource noty_update}" Foreground="{StaticResource Brush_TextBlock_Inactivity}" Height="auto" Width="auto" FontFamily="{StaticResource Jura}" FontSize="13px" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                <ProgressBar x:Name="ProgressDownload" Value="0" Foreground="{StaticResource Brush_Accent}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="340" Height="10"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock TextAlignment="Center" Style="{StaticResource TextDefault}"  Foreground="{StaticResource Brush_TextBlock_Inactivity}"  FontSize="13px" Margin="10,5,0,0">
                       <Run FontFamily="{StaticResource Play}" Text="{Binding ElementName=ProgressDownload, Path=Value, Mode=OneWay,StringFormat=N0}"/>
                       <Run Text="%  ● "/>
                       <Run x:Name="SizeByte" Text="{DynamicResource connection_update}"/>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Grid>

    </Grid>
    
</ui:FluentWindow>
