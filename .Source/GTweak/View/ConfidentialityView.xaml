<UserControl x:Class="GTweak.View.ConfidentialityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GTweak.Core.ViewModel" 
             xmlns:uc="clr-namespace:GTweak.Assets.UserControl"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:GTweak.View"
             mc:Ignorable="d" d:Width="1014" Style="{StaticResource LaunchPage}">
    <UserControl.DataContext>
        <vm:ConfidentialityViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="uc:ToggleButton" x:Key="ToggleStyle">
            <Setter Property="Height" Value="23"/>
            <EventSetter Event="MouseEnter" Handler="Tweak_MouseEnter"/>
            <EventSetter Event="MouseLeave" Handler="Tweak_MouseLeave"/>
            <EventSetter Event="ChangedState" Handler="TglButton_ChangedState"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="130" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">

                <uc:FlowPanel VerticalSpacing="20" HorizontalSpacing="5" Margin="10">

                    <uc:ToggleButton x:Name="TglButton1" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton1].State}" TextResource="{DynamicResource tgl1_conf}" Description="{DynamicResource tgl1_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton2" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton2].State}" TextResource="{DynamicResource tgl2_conf}" Description="{DynamicResource tgl2_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton3" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton3].State}" TextResource="{DynamicResource tgl3_conf}" Description="{DynamicResource tgl3_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton4" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton4].State}" TextResource="{DynamicResource tgl4_conf}" Description="{DynamicResource tgl4_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton5" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton5].State}" TextResource="{DynamicResource tgl5_conf}" Description="{DynamicResource tgl5_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton6" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton6].State}" TextResource="{DynamicResource tgl6_conf}" Description="{DynamicResource tgl6_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton7" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton7].State}" TextResource="{DynamicResource tgl7_conf}" Description="{DynamicResource tgl7_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton8" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton8].State}" TextResource="{DynamicResource tgl8_conf}" Description="{DynamicResource tgl8_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton9" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton9].State}" TextResource="{DynamicResource tgl9_conf}" Description="{DynamicResource tgl9_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton10" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton10].State}" TextResource="{DynamicResource tgl10_conf}" Description="{DynamicResource tgl10_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton11" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton11].State}" TextResource="{DynamicResource tgl11_conf}" Description="{DynamicResource tgl11_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton12" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton12].State}" TextResource="{DynamicResource tgl12_conf}" Description="{DynamicResource tgl12_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton13" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton13].State}" TextResource="{DynamicResource tgl13_conf}" Description="{DynamicResource tgl13_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton14" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton14].State}" TextResource="{DynamicResource tgl14_conf}" Description="{DynamicResource tgl14_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton15" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton15].State}" TextResource="{DynamicResource tgl15_conf}" Description="{DynamicResource tgl15_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton16" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton16].State}" TextResource="{DynamicResource tgl16_conf}" Description="{DynamicResource tgl16_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton17" Visibility="{Binding NvSupportAvailable}" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton17].State}" TextResource="{DynamicResource tgl17_conf}" Description="{DynamicResource tgl17_desc_conf}" />
                    <uc:ToggleButton x:Name="TglButton18" Style="{StaticResource ToggleStyle}" StateNA="{Binding [TglButton18].State}" TextResource="{DynamicResource tgl18_conf}" Description="{DynamicResource tgl18_desc_conf}" />

                </uc:FlowPanel>

            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="1">
            <uc:DescriptionBlock VerticalAlignment="Stretch" x:Name="DescBlock" DefaultText="{DynamicResource defaultDescription}"/>
        </Grid>

    </Grid>
</UserControl>
