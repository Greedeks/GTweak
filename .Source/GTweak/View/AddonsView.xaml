<UserControl x:Class="GTweak.View.AddonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="clr-namespace:GTweak.Behaviors"
             xmlns:vm="clr-namespace:GTweak.Core.ViewModel"
             xmlns:uc="clr-namespace:GTweak.Assets.UserControls"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:GTweak.View"
             mc:Ignorable="d" d:Width="1014" Style="{StaticResource LaunchPage}">
    <UserControl.DataContext>
        <vm:AddonsViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10,5,20,0">
            <Border Background="{StaticResource Brush_ShadowOverlay}" Opacity="{StaticResource DataElement_Opacity}" IsHitTestVisible="False" CornerRadius="8">
                <Border.RenderTransform>
                    <TranslateTransform X="3" Y="4"/>
                </Border.RenderTransform>
            </Border>

            <Border Background="{StaticResource Brush_Background_Card}" BorderBrush="{StaticResource Brush_Separator}" BorderThickness="1" CornerRadius="8" Padding="12,7">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <CheckBox Grid.Column="0" VerticalAlignment="Center" IsChecked="{Binding IsRunAsTrustedInstaller}" Content="{DynamicResource chk_run_ti_adds}"/>

                    <Button Grid.Column="1" Command="{Binding UpdateCommand}" Style="{StaticResource ButtonContent_Inverted}" Padding="12,5" HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="ArrowClockwise20" FontSize="13" Margin="0,0,5,0"/>
                            <TextBlock Text="{DynamicResource btn_upd}" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ScrollViewer b:ScrollFadeBehavior.EnableFade="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <ItemsControl ItemsSource="{Binding Addons}" VirtualizingPanel.IsVirtualizing="True">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <uc:FlowPanel Margin="10,10,20,10" EqualizeItemSize="True" ItemWidth="200" UseMasonryLayout="True" HorizontalSpacing="5" VerticalSpacing="5" ContentAlignment="Stretch" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <uc:FileCard FileName="{Binding FileName}" IconSource="{Binding IconImage}" Command="{Binding RunCommand}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Grid x:Name="EmptyStateGrid">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Addons.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ui:SymbolIcon Opacity="0" Symbol="FolderProhibited20" Foreground="{StaticResource Brush_TextBlock_Inactivity}" FontSize="55" Margin="5,0,5,0" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0">
                                <ui:SymbolIcon.RenderTransform>
                                    <ScaleTransform ScaleX="0.5" ScaleY="0.5"/>
                                </ui:SymbolIcon.RenderTransform>
                                <ui:SymbolIcon.Triggers>
                                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard Timeline.DesiredFrameRate="120">
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6" BeginTime="0:0:0.1">
                                                    <DoubleAnimation.EasingFunction>
                                                        <QuadraticEase/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" From="0.5" To="1" Duration="0:0:0.5" DecelerationRatio="0.5" BeginTime="0:0:0.1">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" From="0.5" To="1" Duration="0:0:0.5" DecelerationRatio="0.5" BeginTime="0:0:0.1">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </ui:SymbolIcon.Triggers>
                            </ui:SymbolIcon>

                            <TextBlock x:Name="EmptyStateText" Width="Auto" Height="Auto" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{StaticResource Jura}" FontSize="17pt" Foreground="{StaticResource Brush_TextBlock_Inactivity}" Grid.Row="1" Margin="5,15,5,0" Opacity="0" RenderTransformOrigin="0.5,0.5">
                                <TextBlock.RenderTransform>
                                    <TranslateTransform Y="20"/>
                                </TextBlock.RenderTransform>
                                    <Run Text="{DynamicResource text_info_adds}"/><LineBreak/>
                                    <Run FontSize="15pt" Text="{DynamicResource scripts_info_adds}"/>
                                <TextBlock.Triggers>
                                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard Timeline.DesiredFrameRate="120">
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6" BeginTime="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <QuadraticEase/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y" From="20" To="0" Duration="0:0:0.5" DecelerationRatio="0.7" BeginTime="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </TextBlock.Triggers>
                            </TextBlock>
                        </Grid>
                    </Grid>
                </Grid>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="2" Margin="10,0,20,5">
            <Border Background="{StaticResource Brush_ShadowOverlay}" Opacity="{StaticResource DataElement_Opacity}" IsHitTestVisible="False" CornerRadius="8">
                <Border.RenderTransform>
                    <TranslateTransform X="3" Y="4"/>
                </Border.RenderTransform>
            </Border>

            <Border Background="{StaticResource Brush_Background_Card}" BorderBrush="{StaticResource Brush_Separator}" BorderThickness="1" CornerRadius="8" Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" VerticalAlignment="Center" Margin="0,0,15,0" HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <ui:SymbolIcon Grid.Column="0" Symbol="Folder20" FontSize="15" Foreground="{StaticResource Brush_Inactive}" VerticalAlignment="Center" Margin="0,0,8,0"/>

                        <TextBlock Grid.Column="1" Style="{StaticResource TextDefault}" Foreground="{StaticResource Brush_Inactive}" FontSize="13" VerticalAlignment="Center" Margin="0,0,4,0">
                            <Run Text="{DynamicResource label_directory_adds}"/><Run Text=":" />
                        </TextBlock>

                        <TextBlock Grid.Column="2" Text="{Binding DirectoryPath, Mode=OneWay}" FontSize="13" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" ToolTip="{Binding DirectoryPath}" Background="Transparent" Cursor="Hand">
                            <TextBlock.InputBindings>
                                <MouseBinding MouseAction="LeftClick" Command="{Binding OpenFolderCommand}" />
                            </TextBlock.InputBindings>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource TextDefault}">
                                    <Setter Property="Foreground" Value="{StaticResource Brush_TextBlock_Inactivity}"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource Brush_Highlighted}"/>
                                            <Setter Property="TextDecorations" Value="Underline"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ui:SymbolIcon Symbol="DocumentMultiple20" FontSize="15" Foreground="{StaticResource Brush_Inactive}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock Style="{StaticResource TextDefault}" Foreground="{StaticResource Brush_Inactive}" FontSize="13" VerticalAlignment="Center">
                            <Run Text="{DynamicResource label_count_adds}"/><Run Text=": "/>
                            <Run Text="{Binding Addons.Count, Mode=OneWay}" Foreground="{StaticResource Brush_TextBlock_Inactivity}"/>
                        </TextBlock>
                    </StackPanel>

                    <Button Grid.Column="2" Command="{Binding SelectFolderCommand}" Style="{StaticResource ButtonContent}" Padding="12,5" HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="Edit20" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock VerticalAlignment="Center" Text="{DynamicResource btn_change}" FontSize="13"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>