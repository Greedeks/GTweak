<UserControl x:Class="GTweak.View.ToolsetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GTweak.Core.ViewModel"
             xmlns:uc="clr-namespace:GTweak.Assets.UserControls"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:GTweak.View"
             mc:Ignorable="d" d:Width="1014" Style="{StaticResource LaunchPage}">
    <UserControl.DataContext>
        <vm:ToolsetViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <uc:FlowPanel Grid.Row="0" ContentAlignment="Left" Orientation="Horizontal" HorizontalSpacing="10" VerticalSpacing="5" Margin="10,5,10,0">
            <Button Command="{Binding SelectFolderCommand}" Style="{StaticResource ButtonContent}" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:SymbolIcon Symbol="FolderOpen20" FontSize="15" Margin="0,0,5,0"/>
                    <TextBlock VerticalAlignment="Center">
                        <Run Text="{DynamicResource btn_open_dir}"/><Run Text="..."/>
                    </TextBlock>
                </StackPanel>
            </Button>

            <Border Grid.Column="0" BorderThickness="1" BorderBrush="{StaticResource Brush_Separator}" Height="29" CornerRadius="6" Background="Transparent" VerticalAlignment="Stretch" Padding="10,0" Cursor="Hand">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding OpenFolderCommand}"/>
                </Border.InputBindings>
                <TextBlock Style="{StaticResource Description}" FontSize="14px" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="0,5" TextTrimming="CharacterEllipsis" Cursor="Hand">
                    <Run Text="{DynamicResource download_folder_ts}"/><Run Text=": "/><Run Text="{Binding DownloadPath}" Foreground="{StaticResource Brush_Inactive}"/>
                </TextBlock>
            </Border>
            
        </uc:FlowPanel>

        <Border Grid.Row="1" Background="{StaticResource Brush_Separator}" Height="1.2" Margin="10,10,10,0" CornerRadius="10" HorizontalAlignment="Stretch"/>

        <Grid Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <ItemsControl ItemsSource="{Binding Tools}" VirtualizingPanel.IsVirtualizing="True">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <uc:FlowPanel Margin="10,10,10,20" HorizontalSpacing="5" VerticalSpacing="10" ContentAlignment="Stretch" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <uc:ToolsetCard AppName="{Binding AppName}" AppIcon="{Binding AppIconSource}" AuthorName="{Binding AuthorName}" AuthorIcon="{Binding AuthorIconSource}" SourceUrl="{Binding SourceUrl}" DownloadCommand="{Binding DownloadCommand}" CancelCommand="{Binding CancelCommand}" IsDownloading="{Binding IsDownloading}" Progress="{Binding Progress}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
