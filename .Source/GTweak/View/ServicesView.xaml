<UserControl x:Class="GTweak.View.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="clr-namespace:GTweak.Behaviors"
             xmlns:vm="clr-namespace:GTweak.Core.ViewModel"  
             xmlns:uc="clr-namespace:GTweak.Assets.UserControls"
             xmlns:local="clr-namespace:GTweak.View"
             mc:Ignorable="d" d:Width="1014" Style="{StaticResource LaunchPage}">
    <UserControl.DataContext>
        <vm:ServicesViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="uc:ToggleButton" x:Key="ToggleStyle">
            <Setter Property="Height" Value="23"/>
            <EventSetter Event="MouseEnter" Handler="Tweak_MouseEnter"/>
            <EventSetter Event="MouseLeave" Handler="Tweak_MouseLeave"/>
            <EventSetter Event="ChangedState" Handler="TglButton_ChangedState"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="135" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <ScrollViewer b:ScrollFadeBehavior.EnableFade="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <uc:FlowPanel VerticalSpacing="20" HorizontalSpacing="5" Margin="10">

                    <uc:ToggleButton x:Name="WindowsSearch" Style="{StaticResource ToggleStyle}" State="{Binding [WindowsSearch].State}" TextResource="{DynamicResource tgl_WindowsSearch_svc}" Description="{DynamicResource desc_WindowsSearch_svc}" />
                    <uc:ToggleButton x:Name="Xbox" Style="{StaticResource ToggleStyle}" State="{Binding [Xbox].State}" TextResource="{DynamicResource tgl_Xbox_svc}" Description="{DynamicResource desc_Xbox_svc}" />
                    <uc:ToggleButton x:Name="WindowsUpdate" Style="{StaticResource ToggleStyle}" State="{Binding [WindowsUpdate].State}" TextResource="{DynamicResource tgl_WindowsUpdate_svc}" Description="{DynamicResource desc_WindowsUpdate_svc}" />
                    <uc:ToggleButton x:Name="WindowsStore" Style="{StaticResource ToggleStyle}" State="{Binding [WindowsStore].State}" TextResource="{DynamicResource tgl_WindowsStore_svc}" Description="{DynamicResource desc_WindowsStore_svc}" />
                    <uc:ToggleButton x:Name="PerformanceCounters" Style="{StaticResource ToggleStyle}" State="{Binding [PerformanceCounters].State}" TextResource="{DynamicResource tgl_PerformanceCounters_svc}" Description="{DynamicResource desc_PerformanceCounters_svc}" />
                    <uc:ToggleButton x:Name="Biometrics" Style="{StaticResource ToggleStyle}" State="{Binding [Biometrics].State}" TextResource="{DynamicResource tgl_Biometrics_svc}" Description="{DynamicResource desc_Biometrics_svc}" />
                    <uc:ToggleButton x:Name="Bluetooth" Style="{StaticResource ToggleStyle}" State="{Binding [Bluetooth].State}" TextResource="{DynamicResource tgl_Bluetooth_svc}" Description="{DynamicResource desc_Bluetooth_svc}" />
                    <uc:ToggleButton x:Name="Printers" Style="{StaticResource ToggleStyle}" State="{Binding [Printers].State}" TextResource="{DynamicResource tgl_Printers_svc}" Description="{DynamicResource desc_Printers_svc}" />
                    <uc:ToggleButton x:Name="Scanner" Style="{StaticResource ToggleStyle}" State="{Binding [Scanner].State}" TextResource="{DynamicResource tgl_Scanner_svc}" Description="{DynamicResource desc_Scanner_svc}" />
                    <uc:ToggleButton x:Name="Fax" Style="{StaticResource ToggleStyle}" State="{Binding [Fax].State}" TextResource="{DynamicResource tgl_Fax_svc}" Description="{DynamicResource desc_Fax_svc}" />
                    <uc:ToggleButton x:Name="TabletMobileMode" Style="{StaticResource ToggleStyle}" State="{Binding [TabletMobileMode].State}" TextResource="{DynamicResource tgl_TabletMobileMode_svc}" Description="{DynamicResource desc_TabletMobileMode_svc}" />
                    <uc:ToggleButton x:Name="SecondaryMonitors" Style="{StaticResource ToggleStyle}" State="{Binding [SecondaryMonitors].State}" TextResource="{DynamicResource tgl_SecondaryMonitors_svc}" Description="{DynamicResource desc_SecondaryMonitors_svc}" />
                    <uc:ToggleButton x:Name="SystemComponents" Style="{StaticResource ToggleStyle}" State="{Binding [SystemComponents].State}" TextResource="{DynamicResource tgl_SystemComponents_svc}" Description="{DynamicResource desc_SystemComponents_svc}" />
                    <uc:ToggleButton x:Name="LocalNetwork" Style="{StaticResource ToggleStyle}" State="{Binding [LocalNetwork].State}" TextResource="{DynamicResource tgl_LocalNetwork_svc}" Description="{DynamicResource desc_LocalNetwork_svc}" />
                    <uc:ToggleButton x:Name="UsbModemsRouters" Style="{StaticResource ToggleStyle}" State="{Binding [UsbModemsRouters].State}" TextResource="{DynamicResource tgl_UsbModemsRouters_svc}" Description="{DynamicResource desc_UsbModemsRouters_svc}" />
                    <uc:ToggleButton x:Name="VpnClients" Style="{StaticResource ToggleStyle}" State="{Binding [VpnClients].State}" TextResource="{DynamicResource tgl_VpnClients_svc}" Description="{DynamicResource desc_VpnClients_svc}" />
                    <uc:ToggleButton x:Name="WindowsMedia" Style="{StaticResource ToggleStyle}" State="{Binding [WindowsMedia].State}" TextResource="{DynamicResource tgl_WindowsMedia_svc}" Description="{DynamicResource desc_WindowsMedia_svc}" />
                    <uc:ToggleButton x:Name="RemoteDesktop" Style="{StaticResource ToggleStyle}" State="{Binding [RemoteDesktop].State}" TextResource="{DynamicResource tgl_RemoteDesktop_svc}" Description="{DynamicResource desc_RemoteDesktop_svc}" />
                    <uc:ToggleButton x:Name="ErrorEventLogging" Style="{StaticResource ToggleStyle}" State="{Binding [ErrorEventLogging].State}" TextResource="{DynamicResource tgl_ErrorEventLogging_svc}" Description="{DynamicResource desc_ErrorEventLogging_svc}" />
                    <uc:ToggleButton x:Name="WebDav" Style="{StaticResource ToggleStyle}" State="{Binding [WebDav].State}" TextResource="{DynamicResource tgl_WebDav_svc}" Description="{DynamicResource desc_WebDav_svc}" />
                    <uc:ToggleButton x:Name="SmartCards" Style="{StaticResource ToggleStyle}" State="{Binding [SmartCards].State}" TextResource="{DynamicResource tgl_SmartCards_svc}" Description="{DynamicResource desc_SmartCards_svc}" />
                    <uc:ToggleButton x:Name="WindowsKiosk" Style="{StaticResource ToggleStyle}" State="{Binding [WindowsKiosk].State}" TextResource="{DynamicResource tgl_WindowsKiosk_svc}" Description="{DynamicResource desc_WindowsKiosk_svc}" />
                    <uc:ToggleButton x:Name="FileEncryption" Style="{StaticResource ToggleStyle}" State="{Binding [FileEncryption].State}" TextResource="{DynamicResource tgl_FileEncryption_svc}" Description="{DynamicResource desc_FileEncryption_svc}" />
                    <uc:ToggleButton x:Name="WindowsLocalization" Style="{StaticResource ToggleStyle}" State="{Binding [WindowsLocalization].State}" TextResource="{DynamicResource tgl_WindowsLocalization_svc}" Description="{DynamicResource desc_WindowsLocalization_svc}" />
                    <uc:ToggleButton x:Name="BackgroundSecurityCheck" Style="{StaticResource ToggleStyle}" State="{Binding [BackgroundSecurityCheck].State}" TextResource="{DynamicResource tgl_BackgroundSecurityCheck_svc}" Description="{DynamicResource desc_BackgroundSecurityCheck_svc}" />
                    <uc:ToggleButton x:Name="BackgroundDiagnostics" Style="{StaticResource ToggleStyle}" State="{Binding [BackgroundDiagnostics].State}" TextResource="{DynamicResource tgl_BackgroundDiagnostics_svc}" Description="{DynamicResource desc_BackgroundDiagnostics_svc}" />
                    <uc:ToggleButton x:Name="CorporateTools" Style="{StaticResource ToggleStyle}" State="{Binding [CorporateTools].State}" TextResource="{DynamicResource tgl_CorporateTools_svc}" Description="{DynamicResource desc_CorporateTools_svc}" />
                    <uc:ToggleButton x:Name="HyperV" Style="{StaticResource ToggleStyle}" State="{Binding [HyperV].State}" TextResource="{DynamicResource tgl_HyperV_svc}" Description="{DynamicResource desc_HyperV_svc}" />
                    <uc:ToggleButton x:Name="PushNotifications" Style="{StaticResource ToggleStyle}" State="{Binding [PushNotifications].State}" TextResource="{DynamicResource tgl_PushNotifications_svc}" Description="{DynamicResource desc_PushNotifications_svc}" />
                    <uc:ToggleButton x:Name="RetailDemo" Style="{StaticResource ToggleStyle}" State="{Binding [RetailDemo].State}" TextResource="{DynamicResource tgl_RetailDemo_svc}" Description="{DynamicResource desc_RetailDemo_svc}" />
                    <uc:ToggleButton x:Name="EdgeUpdates" Visibility="{Binding EdgeAvailable}" Style="{StaticResource ToggleStyle}" State="{Binding [EdgeUpdates].State}" TextResource="{DynamicResource tgl_EdgeUpdates_svc}" Description="{DynamicResource desc_EdgeUpdates_svc}" />
                    <uc:ToggleButton x:Name="MultimediaClassScheduler" Style="{StaticResource ToggleStyle}" State="{Binding [MultimediaClassScheduler].State}" TextResource="{DynamicResource tgl_MultimediaClassScheduler_svc}" Description="{DynamicResource desc_MultimediaClassScheduler_svc}" />
                    <uc:ToggleButton x:Name="Geolocation" Style="{StaticResource ToggleStyle}" State="{Binding [Geolocation].State}" TextResource="{DynamicResource tgl_Geolocation_svc}" Description="{DynamicResource desc_Geolocation_svc}" />
                    
                </uc:FlowPanel>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="1">
            <uc:DescriptionBlock VerticalAlignment="Stretch" x:Name="DescBlock" DefaultText="{DynamicResource default_desc}"/>
        </Grid>
        
    </Grid>
</UserControl>
