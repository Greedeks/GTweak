<UserControl x:Class="GTweak.View.InterfaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="clr-namespace:GTweak.Behaviors"
             xmlns:vm="clr-namespace:GTweak.Core.ViewModel" 
             xmlns:uc="clr-namespace:GTweak.Assets.UserControls"
             xmlns:local="clr-namespace:GTweak.View"
             mc:Ignorable="d" d:Width="1014" Style="{StaticResource LaunchPage}">
    <UserControl.DataContext>
        <vm:InterfaceViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="uc:ToggleButton" x:Key="ToggleStyle">
            <Setter Property="Height" Value="23"/>
            <EventSetter Event="MouseEnter" Handler="Tweak_MouseEnter"/>
            <EventSetter Event="MouseLeave" Handler="Tweak_MouseLeave"/>
            <EventSetter Event="ChangedState" Handler="TglButton_ChangedState"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="135" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <ScrollViewer b:ScrollFadeBehavior.EnableFade="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <uc:FlowPanel VerticalSpacing="20" HorizontalSpacing="5" Margin="10">

                    <StackPanel Orientation="Vertical" Margin="0,-10,0,-10">
                        <StackPanel Orientation="Horizontal" Height="45"  MouseEnter="Tweak_MouseEnter" MouseLeave="Tweak_MouseLeave" ToolTip="{DynamicResource desc_CursorSelection_intf}" ToolTipService.IsEnabled="False" Background="Transparent">
                            <TextBlock Style="{StaticResource TextDefault}" VerticalAlignment="Center" Text="{DynamicResource CursorSelection_intf}"/>
                            <uc:ColorPicker x:Name="CursorSelection" SelectedColorString="{Binding [CursorSelection].Value}" DefaultColor="#0077D4" Margin="7,5,5,0" Height="32" VerticalAlignment="Center" ColorPicked="ColorPicker_ColorPicked"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Height="45" MouseEnter="Tweak_MouseEnter" MouseLeave="Tweak_MouseLeave" ToolTip="{DynamicResource desc_Tooltip_intf}" ToolTipService.IsEnabled="False" Background="Transparent">
                            <TextBlock Style="{StaticResource TextDefault}" VerticalAlignment="Center" Text="{DynamicResource Tooltip_intf}"/>
                            <uc:ColorPicker x:Name="Tooltip" SelectedColorString="{Binding [Tooltip].Value}" DefaultColor="#FFFFE1" Margin="7,5,5,0" Height="32" VerticalAlignment="Center" ColorPicked="ColorPicker_ColorPicked"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Height="45" MouseEnter="Tweak_MouseEnter" MouseLeave="Tweak_MouseLeave" Visibility="{Binding Win11FeatureOnly}" ToolTip="{DynamicResource desc_NavigationItems_intf}" ToolTipService.IsEnabled="False" Background="Transparent">
                            <TextBlock Style="{StaticResource TextDefault}" VerticalAlignment="Center" Text="{DynamicResource NavigationItems_intf}"/>
                            <uc:ExpandableBox Text="{DynamicResource btn_change}" Margin="7,5,5,0" Height="32" VerticalAlignment="Center" Closed="ExpandableBox_Closed">
                                <CheckBox x:Uid="ExplorerHome" IsChecked="{Binding [ExplorerHome].State}" Click="CheckBox_Click">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[Home]}" Width="16" Height="16"/>
                                        <TextBlock Text="{DynamicResource ExplorerHome_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="ExplorerGallery" IsChecked="{Binding [ExplorerGallery].State}" Margin="0,4,0,0" Click="CheckBox_Click">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[Gallery]}" Width="16" Height="16"/>
                                        <TextBlock Text="{DynamicResource ExplorerGallery_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="ExplorerOneDrive" IsChecked="{Binding [ExplorerOneDrive].State}" Visibility="{Binding OneDriveAvailable}" Margin="0,4,0,0" Click="CheckBox_Click">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[OneDrive]}" Width="18" Height="18"/>
                                        <TextBlock Text="{DynamicResource ExplorerOneDrive_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                            </uc:ExpandableBox>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Margin="0,-10,0,-10">
                        <StackPanel Orientation="Horizontal" Height="45" MouseEnter="Tweak_MouseEnter" MouseLeave="Tweak_MouseLeave" ToolTip="{DynamicResource desc_FileExplorerDisplay_intf}" ToolTipService.IsEnabled="False" Background="Transparent">
                            <TextBlock Style="{StaticResource TextDefault}" VerticalAlignment="Center" Text="{DynamicResource FileExplorerDisplay_intf}"/>
                            <uc:ExpandableBox Text="{DynamicResource btn_change}" Margin="7,5,5,0" Height="32" VerticalAlignment="Center" Closed="ExpandableBox_Closed">
                                <CheckBox x:Uid="HiddenFiles" Content="{DynamicResource HiddenFiles_intf}" IsChecked="{Binding [HiddenFiles].State}" Click="CheckBox_Click"/>
                                <CheckBox x:Uid="SystemFiles" Content="{DynamicResource SystemFiles_intf}" IsChecked="{Binding [SystemFiles].State}" Margin="0,4,0,0" Click="CheckBox_Click"/>
                                <CheckBox x:Uid="FileExtensions" Content="{DynamicResource FileExtensions_intf}" IsChecked="{Binding [FileExtensions].State}" Margin="0,4,0,0" Click="CheckBox_Click"/>
                                <CheckBox x:Uid="EmptyDrives" Content="{DynamicResource EmptyDrives_intf}" IsChecked="{Binding [EmptyDrives].State}" Margin="0,4,0,0" Click="CheckBox_Click"/>
                            </uc:ExpandableBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Height="45" MouseEnter="Tweak_MouseEnter" MouseLeave="Tweak_MouseLeave" ToolTip="{DynamicResource desc_DesktopIcons_intf}" ToolTipService.IsEnabled="False" Background="Transparent">
                            <TextBlock Style="{StaticResource TextDefault}" VerticalAlignment="Center" Text="{DynamicResource DesktopIcons_intf}"/>
                            <uc:ExpandableBox Text="{DynamicResource btn_change}" Margin="7,5,5,0" Height="32" VerticalAlignment="Center" Closed="ExpandableBox_Closed">
                                <CheckBox x:Uid="IconComputer" IsChecked="{Binding [IconComputer].State}" Click="CheckBox_Click">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[PC]}" Width="16" Height="16"/>
                                        <TextBlock Text="{DynamicResource IconComputer_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="IconNetwork" IsChecked="{Binding [IconNetwork].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[Network]}" Width="16" Height="16"/>
                                        <TextBlock Text="{DynamicResource IconNetwork_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="IconRecycleBin" IsChecked="{Binding [IconRecycleBin].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[Trash]}" Width="16" Height="16"/>
                                        <TextBlock Text="{DynamicResource IconRecycleBin_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="IconControlPanel" IsChecked="{Binding [IconControlPanel].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[Panel]}" Width="16" Height="16"/>
                                        <TextBlock Text="{DynamicResource IconControlPanel_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="IconUserFiles" IsChecked="{Binding [IconUserFiles].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[UserFile]}" Width="16" Height="16"/>
                                        <TextBlock Text="{DynamicResource IconUserFiles_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="IconOneDrive" IsChecked="{Binding [IconOneDrive].State}" Visibility="{Binding OneDriveAvailable}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[OneDriveFile]}" Width="17" Height="17"/>
                                        <TextBlock Text="{DynamicResource IconOneDrive_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                            </uc:ExpandableBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Height="45" MouseEnter="Tweak_MouseEnter" MouseLeave="Tweak_MouseLeave" ToolTip="{DynamicResource desc_ThisPCFolders_intf}" ToolTipService.IsEnabled="False" Background="Transparent">
                            <TextBlock Style="{StaticResource TextDefault}" VerticalAlignment="Center" Text="{DynamicResource ThisPCFolders_intf}"/>
                            <uc:ExpandableBox Text="{DynamicResource btn_change}" Margin="7,5,5,0" Height="32" VerticalAlignment="Center" Closed="ExpandableBox_Closed">
                                <CheckBox x:Uid="FolderObjects3D" IsChecked="{Binding [FolderObjects3D].State}" Click="CheckBox_Click">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[FolderObjects3D]}" Width="18" Height="18"/>
                                        <TextBlock Text="{DynamicResource FolderObjects3D_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="FolderDesktop" IsChecked="{Binding [FolderDesktop].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[FolderDesktop]}" Width="18" Height="18"/>
                                        <TextBlock Text="{DynamicResource FolderDesktop_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="FolderDownloads" IsChecked="{Binding [FolderDownloads].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[FolderDownloads]}" Width="18" Height="18"/>
                                        <TextBlock Text="{DynamicResource FolderDownloads_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="FolderDocuments" IsChecked="{Binding [FolderDocuments].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[FolderDocuments]}" Width="18" Height="18"/>
                                        <TextBlock Text="{DynamicResource FolderDocuments_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="FolderPictures" IsChecked="{Binding [FolderPictures].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[FolderPictures]}" Width="18" Height="18"/>
                                        <TextBlock Text="{DynamicResource FolderPictures_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="FolderMusic" IsChecked="{Binding [FolderMusic].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[FolderMusic]}" Width="18" Height="18"/>
                                        <TextBlock Text="{DynamicResource FolderMusic_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                                <CheckBox x:Uid="FolderVideo" IsChecked="{Binding [FolderVideo].State}" Click="CheckBox_Click" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" Background="Transparent">
                                        <Image Source="{Binding Icons[FolderVideo]}" Width="18" Height="18"/>
                                        <TextBlock Text="{DynamicResource FolderVideo_intf}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                            </uc:ExpandableBox>
                        </StackPanel>
                    </StackPanel>

                    <uc:ToggleButton x:Name="SystemButtonSize" Style="{StaticResource ToggleStyle}" State="{Binding [SystemButtonSize].State}" TextResource="{DynamicResource SystemButtonSize_intf}" Description="{DynamicResource desc_SystemButtonSize_intf}" />
                    <uc:ToggleButton x:Name="CursorFlickerFrequency" Style="{StaticResource ToggleStyle}" State="{Binding [CursorFlickerFrequency].State}" TextResource="{DynamicResource CursorFlickerFrequency_intf}" Description="{DynamicResource desc_CursorFlickerFrequency_intf}" />
                    <uc:ToggleButton x:Name="ScrollbarSize" Style="{StaticResource ToggleStyle}" State="{Binding [ScrollbarSize].State}" TextResource="{DynamicResource ScrollbarSize_intf}" Description="{DynamicResource desc_ScrollbarSize_intf}" />
                    <uc:ToggleButton x:Name="TaskbarTransparency" Style="{StaticResource ToggleStyle}" State="{Binding [TaskbarTransparency].State}" TextResource="{DynamicResource TaskbarTransparency_intf}" Description="{DynamicResource desc_TaskbarTransparency_intf}" />
                    <uc:ToggleButton x:Name="TaskbarDarkTheme" Style="{StaticResource ToggleStyle}" State="{Binding [TaskbarDarkTheme].State}" TextResource="{DynamicResource TaskbarDarkTheme_intf}" Description="{DynamicResource desc_TaskbarDarkTheme_intf}" />
                    <uc:ToggleButton x:Name="AppDarkTheme" Style="{StaticResource ToggleStyle}" State="{Binding [AppDarkTheme].State}" TextResource="{DynamicResource AppDarkTheme_intf}" Description="{DynamicResource desc_AppDarkTheme_intf}" />
                    <uc:ToggleButton x:Name="ContextMenuDelay" Style="{StaticResource ToggleStyle}" State="{Binding [ContextMenuDelay].State}" TextResource="{DynamicResource ContextMenuDelay_intf}" Description="{DynamicResource desc_ContextMenuDelay_intf}" />
                    <uc:ToggleButton x:Name="TaskbarPreviewDelay" Style="{StaticResource ToggleStyle}" State="{Binding [TaskbarPreviewDelay].State}" TextResource="{DynamicResource TaskbarPreviewDelay_intf}" Description="{DynamicResource desc_TaskbarPreviewDelay_intf}" />
                    <uc:ToggleButton x:Name="IconOverlayBadges" Style="{StaticResource ToggleStyle}" State="{Binding [IconOverlayBadges].State}" TextResource="{DynamicResource IconOverlayBadges_intf}" Description="{DynamicResource desc_IconOverlayBadges_intf}" />
                    <uc:ToggleButton x:Name="ShortcutNameSuffix" Style="{StaticResource ToggleStyle}" State="{Binding [ShortcutNameSuffix].State}" TextResource="{DynamicResource ShortcutNameSuffix_intf}" Description="{DynamicResource desc_ShortcutNameSuffix_intf}" />
                    <uc:ToggleButton x:Name="TaskbarAlignment" Style="{StaticResource ToggleStyle}" State="{Binding [TaskbarAlignment].State}" TextResource="{DynamicResource TaskbarAlignment_intf}" Description="{DynamicResource desc_TaskbarAlignment_intf}" />
                    <uc:ToggleButton x:Name="StartMenuLayout" Style="{StaticResource ToggleStyle}" State="{Binding [StartMenuLayout].State}" TextResource="{DynamicResource StartMenuLayout_intf}" Description="{DynamicResource desc_StartMenuLayout_intf}" />
                    <uc:ToggleButton x:Name="CompactContextMenu" Style="{StaticResource ToggleStyle}" State="{Binding [CompactContextMenu].State}" TextResource="{DynamicResource CompactContextMenu_intf}" Description="{DynamicResource desc_CompactContextMenu_intf}" />
                    <uc:ToggleButton x:Name="TipsSuggestions" Style="{StaticResource ToggleStyle}" State="{Binding [TipsSuggestions].State}" TextResource="{DynamicResource TipsSuggestions_intf}" Description="{DynamicResource desc_TipsSuggestions_intf}" />
                    <uc:ToggleButton x:Name="CompactExplorerMode" Visibility="{Binding Win11FeatureOnly}" Style="{StaticResource ToggleStyle}" State="{Binding [CompactExplorerMode].State}" TextResource="{DynamicResource CompactExplorerMode_intf}" Description="{DynamicResource desc_CompactExplorerMode_intf}" />
                    <uc:ToggleButton x:Name="CopilotRecall" Visibility="{Binding Win11FeatureOnly}" Style="{StaticResource ToggleStyle}" State="{Binding [CopilotRecall].State}" TextResource="{DynamicResource CopilotRecall_intf}" Description="{DynamicResource desc_CopilotRecall_intf}" />
                    <uc:ToggleButton x:Name="TaskbarEndTask" Visibility="{Binding Win11FeatureOnly}" Style="{StaticResource ToggleStyle}" State="{Binding [TaskbarEndTask].State}" TextResource="{DynamicResource TaskbarEndTask_intf}" Description="{DynamicResource desc_TaskbarEndTask_intf}" />
                    <uc:ToggleButton x:Name="SnapLayouts" Visibility="{Binding Win11FeatureOnly}" Style="{StaticResource ToggleStyle}" State="{Binding [SnapLayouts].State}" TextResource="{DynamicResource SnapLayouts_intf}" Description="{DynamicResource desc_SnapLayouts_intf}" />
                    <uc:ToggleButton x:Name="TaskbarIconsButtons" Visibility="{Binding Win11FeatureOnly}" Style="{StaticResource ToggleStyle}" State="{Binding [TaskbarIconsButtons].State}" TextResource="{DynamicResource TaskbarIconsButtons_intf}" Description="{DynamicResource desc_TaskbarIconsButtons_intf}" />
                    <uc:ToggleButton x:Name="PersonalizedAds" Visibility="{Binding Win11FeatureOnly}" Style="{StaticResource ToggleStyle}" State="{Binding [PersonalizedAds].State}" TextResource="{DynamicResource PersonalizedAds_intf}" Description="{DynamicResource desc_PersonalizedAds_intf}" />
                    <uc:ToggleButton x:Name="RestoreFolderWindows" Visibility="{Binding Win11FeatureAvailable}" Style="{StaticResource ToggleStyle}" State="{Binding [RestoreFolderWindows].State}" TextResource="{DynamicResource RestoreFolderWindows_intf}" Description="{DynamicResource desc_RestoreFolderWindows_intf}" />
                    <uc:ToggleButton x:Name="TipsNotifications" Visibility="{Binding Win11FeatureOnly}" Style="{StaticResource ToggleStyle}" State="{Binding [TipsNotifications].State}" TextResource="{DynamicResource TipsNotifications_intf}" Description="{DynamicResource desc_TipsNotifications_intf}" />
                    <uc:ToggleButton x:Name="JpegWallpaperCompression" Style="{StaticResource ToggleStyle}" State="{Binding [JpegWallpaperCompression].State}" TextResource="{DynamicResource JpegWallpaperCompression_intf}" Description="{DynamicResource desc_JpegWallpaperCompression_intf}" />
                    <uc:ToggleButton x:Name="DesktopTooltips" Style="{StaticResource ToggleStyle}" State="{Binding [DesktopTooltips].State}" TextResource="{DynamicResource DesktopTooltips_intf}" Description="{DynamicResource desc_DesktopTooltips_intf}" />
                    <uc:ToggleButton x:Name="BingStartSearch" Style="{StaticResource ToggleStyle}" State="{Binding [BingStartSearch].State}" TextResource="{DynamicResource BingStartSearch_intf}" Description="{DynamicResource desc_BingStartSearch_intf}" />
                    <uc:ToggleButton x:Name="OpenQuickAccess" Style="{StaticResource ToggleStyle}" State="{Binding [OpenQuickAccess].State}" TextResource="{DynamicResource OpenQuickAccess_intf}" Description="{DynamicResource desc_OpenQuickAccess_intf}" />
                    <uc:ToggleButton x:Name="AdaptiveBrightness" Style="{StaticResource ToggleStyle}" State="{Binding [AdaptiveBrightness].State}" TextResource="{DynamicResource AdaptiveBrightness_intf}" Description="{DynamicResource desc_AdaptiveBrightness_intf}" />

                </uc:FlowPanel>
            </ScrollViewer>
        </Grid>
        
        <Grid Grid.Row="1">
            <uc:DescriptionBlock VerticalAlignment="Stretch" x:Name="DescBlock" DefaultText="{DynamicResource default_desc}"/>
        </Grid>
    </Grid>
    
</UserControl>
